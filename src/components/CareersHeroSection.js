import React from "react";
import "./CareersHeroSection.css"; // Import the CSS for this component

const CareersHeroSection = () => {
  return (
    <section className="careers-hero-section">
      <div className="careers-overlay">
        <h1 className="careers-title">Build your Career with Euro Group</h1>
        <p className="careers-description">
          We believe that our highly qualified professionals are the key to any
          achievement and success. Therefore, we keep working on enriching their
          professional skills, updating and upgrading their knowledge in order
          to address the daily growing complex challenges and evolution.
        </p>
      </div>
    </section>
  );
};

export default CareersHeroSection;
